trigger: none

pool: Default

steps:

- task: PowerShell@2
  displayName: Terrraform init

  inputs:
    targetType: 'inline'
    script: |
      ls
      terraform init



- task: PowerShell@2
  inputs:
    targetType: 'inline'
    script: |
      terraform init
      terraform fmt
      Terraform validate
      